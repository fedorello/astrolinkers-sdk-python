"""Public resource classes.

Each resource holds a reference to the transport and exposes typed
methods that map 1:1 to an API endpoint. Resource objects are
constructed by the client facade — users never instantiate them
directly.
"""
